# Copy this file to `.env.local` (ignored). No secrets are required for the
# bundled SQLite database, but keep this file up to date if you relocate it.

DATABASE_URL="file:./zanesville-store.db"
POSTGRES_URL=""
PRISMA_DATABASE_URL=""
FACEBOOK_INVENTORY_PATH=""
